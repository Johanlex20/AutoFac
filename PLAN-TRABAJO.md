# Plan de trabajo — AutoFac (frontend)

Lista viva de tareas de diseño/frontend. Se actualiza a medida que avanzamos o surgen tareas nuevas. Para el roadmap completo del proyecto (seguridad, login, productos, OAuth2, despliegue) ver el plan aprobado en `C:\Users\0.0\.claude\plans\snazzy-gathering-manatee.md`.

## En progreso ahora

- [ ] Nada activo ahora mismo — elegir siguiente ítem de "Pendiente / próximo".

## Pendiente / próximo

- [ ] **[PRIORIDAD — fin de semana] CRUD de productos en el admin, reemplazando el espacio de "Profile"/Cliente**. Hoy el ícono de "Profile" en el sidebar (`admin/usuarios/list`, `UserComponent`) es un placeholder vacío sin lógica — hay que convertir esa sección en la pantalla de gestión de productos: listar, crear, actualizar y eliminar, con el mismo patrón visual ya usado en `admin/documentos` (vista de lista + formulario de página completa para crear/editar, confirmación con SweetAlert2 al eliminar). El backend **ya existe y está listo**: `Producto` entity + `GET/POST/PUT/DELETE /api/producto` (protegidos salvo el `GET`, igual que documentos) — ver `AutoFacApi/.../controller/ProductoController.java`. Falta solo la parte de admin en el frontend:
  - Nuevo componente admin (ej. `admin/productos`) con tabla (nombre, categoría, precio, presentación) + botones Editar/Eliminar, y formulario de página completa para crear/editar (nombre, categoría, descripción, presentación, precio, imagen).
  - Decidir qué pasa con el campo `imagenUrl` en el formulario: por ahora los productos sembrados apuntan a `assets/img/envase-20l.jpg`/`envase-4l.webp` (fotos genéricas de envase, no fotos reales por producto) — se podría dejar como un campo de texto (ruta) por ahora, o conectar con subida de archivo como ya existe en `documentos` si se quiere subir fotos reales por producto más adelante.
  - Actualizar el sidebar: cambiar el ícono/label "Profile" (`face`) por algo como "Productos" (ej. ícono `inventory_2` o similar) apuntando a la nueva ruta.
  - Revisar si conviene mover/renombrar la ruta de `/admin/usuarios/list` a algo como `/admin/productos/list`, o dejar la ruta y solo cambiar qué componente sirve — decidir al empezar.
- [ ] **Antes de desplegar al servidor: copiar el perfil de Chrome autenticado**. El perfil persistente (`AutoFacApi/chrome-profile-siigo/`) que evita el SMS solo existe en la máquina local. Hay que copiarlo al volumen Docker `chrome_profile_data` del servidor (ej. `docker cp`) antes de que el cron corra ahí — si no, la primera corrida en el servidor va a pedir SMS y quedarse colgada, porque ahí no hay forma de recibirlo.
- [ ] **La verificación por SMS expira y hay que repetirla periódicamente** — se confirmó que la "confianza" del dispositivo no dura indefinidamente (expiró en menos de 5 días de inactividad en una prueba real). Cuando esto pase, el cron automático va a fallar en silencio hasta que alguien repita el login manual una vez (botón "Capturar Data", ventana visible) para renovar la confianza. No hay forma de eliminar esto del todo con el enfoque actual — es una limitación real a monitorear (revisar logs del servidor de vez en cuando, o considerar una alerta si el cron falla repetidamente).
- [ ] (Opcional, cosmético) Los logs de facturas sin botón "Ver documento" (fallo de envío, notas de crédito, etc.) muestran el error técnico completo de Selenium (`no such element...`) — se podría limpiar a un mensaje simple tipo "X no tiene documento disponible, se omite" si se quiere un log más legible. No es urgente.
- [ ] (Futuro, no urgente) Migrar el bootstrap de `main.ts` de `bootstrapModule(AppModule)` clásico a `bootstrapApplication` standalone — solución de raíz al bug de change detection, en vez de parchear componente por componente.
- [ ] Comitear los cambios sueltos actuales.
- [ ] **Actividad de aprendizaje: dominar Spring Security + JWT desde cero**. El usuario quiere entender a fondo todo lo que se armó en la autenticación (no solo tenerlo funcionando) — pidió una explicación paso a paso, línea por línea: qué archivo se crea primero, en qué orden, para qué sirve cada variable/anotación/método (`@Value`, `SecretKey`, `Jwts.builder()`, `UserDetailsService`, `DaoAuthenticationProvider`, `SecurityFilterChain`, `OncePerRequestFilter`, etc.), y por qué va en ese orden. Después de la explicación, armar ejercicios/retos para que practique construyendo un login+JWT de cero por su cuenta hasta dominarlo ("que después pueda crear logins con los ojos cerrados"). Formato sugerido: documento o sesión dedicada, no mezclarlo con el desarrollo real del proyecto.
- [ ] **Mejorar el diseño visual del login admin** — hoy es un formulario muy básico (funcional, sin estilo cuidado). Aplicar el mismo enfoque de estilos centralizados en `styles.css` usado en el resto del proyecto.
- [ ] **(Futuro, hardening) Evaluar mover el JWT de `localStorage` a una cookie `httpOnly`** — hoy el token vive en `localStorage`, vulnerable solo ante XSS (no ante acceso externo directo). Cambiar a cookie `httpOnly` lo protege de XSS pero exige manejar CSRF en su lugar — evaluar si vale la pena para el alcance de este proyecto.
- [ ] **(Backend, DX) Evitar depender de XAMPP para desarrollo local** — hoy hay que abrir XAMPP y activar manualmente Apache/MySQL antes de correr el proyecto Spring. Buscar una forma de que el entorno de desarrollo levante MySQL sin ese paso manual, para que "correr el proyecto" sea solo ejecutar Spring Boot. Opciones a evaluar (no decidido cuál usar todavía):
  - **Docker Compose solo para MySQL** (ya existe `docker-compose.yml` para producción/despliegue completo — se podría tener un compose mínimo aparte solo con el contenedor de MySQL para desarrollo, sin tocar Apache).
  - **MariaDB4j** (librería Java que embebe un MySQL/MariaDB real dentro del propio proceso de Spring Boot — arranca y para junto con la app, sin instalar nada aparte).
  - **H2 embebido** solo para perfil de desarrollo (motor en memoria compatible con SQL, cero instalación) — más simple pero con riesgo de diferencias de comportamiento frente a MySQL real en producción.
  - Cualquiera de las tres elimina la necesidad de tener XAMPP corriendo aparte; Apache de XAMPP no hace falta en ningún caso porque Spring Boot ya trae su propio servidor embebido (Tomcat).

## En progreso ahora

- [ ] Nada activo ahora mismo — elegir siguiente ítem de "Pendiente / próximo".

## Ya hecho (contexto, no repetir)

- **Rediseño de identidad visual pública (landing + login)**: paleta y tipografía propias para las páginas públicas (azul de marca del panel admin `#008abb` como acento, Archivo para títulos, IBM Plex Sans para cuerpo), separadas de los colores internos del admin. Se restauró la foto de fondo del hero (`f1.jpeg`) a pedido del usuario tras un ajuste inicial que la había quitado sin consultar — lección aprendida: no reemplazar contenido/imágenes ya elegidas por el usuario sin preguntar primero. Sección "Por qué elegirnos" rediseñada con tarjetas de foto + etiqueta (estilo pedido por el usuario con una referencia visual), usando fotos de stock por ahora.
- **(Backend + Frontend) Catálogo de productos real, con datos y precios reales de BlueChemical**: nueva entidad `Producto` (dominio nuevo, no extensión de `Data`) con `GET/POST/PUT/DELETE /api/producto` (`GET` público, resto protegido — mismo patrón que `documento`). `ProductoSeeder` sembró los **13 productos reales** extraídos directamente de `D:\BLUECHEMICAL\LISTA DE PRECIOS\LISTA DE PRECIOS.docx` (nombre, categoría, presentación y precio exactos, nada inventado). Se revisó el PowerPoint de etiquetas (`ETIQUETAS SGA.pptx`) buscando fotos reales de producto, pero solo tenía piezas de diseño de etiqueta (pictogramas de seguridad, clip-art genérico, hasta una captura de escritorio pegada por accidente) — se usaron en su lugar dos fotos reales de envases que el usuario proporcionó (garrafa 20L y envase 4L), asignadas según la presentación real de cada producto. Nueva sección "Nuestros productos" en la landing con tarjetas (imagen, categoría, nombre, descripción, precio COP, presentación) y botón "Agregar al carrito" **solo visual por ahora** (confirmación con SweetAlert2, sin lógica de carrito real) — decisión explícita del usuario dado que el sitio funciona por cotización, no por pago en línea. Pendiente para el fin de semana: la pantalla de administración para gestionar este catálogo (ver arriba, prioridad).
- **(Backend) Sincronización automática de Siigo validada de punta a punta con una corrida real**: tras arreglar 8 bugs reales (imagen Docker sin Chrome real, login sin manejar sesión ya autenticada, selector del botón mal escrito, fila de encabezados confundida con factura, columna de acciones en contenedor aparte del grid — se busca por `row-id`, "Ver documento" abre en pestaña nueva — hay que cambiar el foco de Selenium, clic nativo fallaba en filas tapadas por la paginación — se cambió a clic por JS, y candados de perfil huérfanos por reinicios de DevTools a la mitad de una corrida — se agregó limpieza automática), una corrida real capturó **17 de 18 facturas nuevas** detectadas sin intervención manual (la única que no se capturó era una factura sin botón "Ver documento" disponible — caso válido, no un bug). Se confirmó además que la verificación por SMS expira con el tiempo (no duró ni 5 días de inactividad) — ver pendiente arriba sobre monitoreo.
- **(Backend) Captura automática de facturas Siigo conectada y funcionando en el servidor (no solo en local)**: `sincronizarFacturas()` (headless, ya existía escrita pero no conectada a nada) ahora corre sola vía un job `@Scheduled` (`SiigoSyncScheduler`, nuevo, con `@EnableScheduling` agregado a `AutoFacApiApplication`), a la hora definida en `siigo.sync.cron` (`application.properties`, ${SIIGO_SYNC_CRON:0 0 23 * * *}, zona `siigo.sync.zone`/`SIIGO_SYNC_ZONE=America/Bogota`) — por defecto corre todos los días a las 11pm, ajustable desde el `.env` del servidor sin recompilar (mismo patrón que `CORS_ORIGINS`). Cada corrida detecta solo facturas nuevas (comparando contra `existsByNumeroFactura`) y las captura todas en bucle sin intervención manual; el flujo manual (`/api/data/capturar`, ventana de Chrome visible) sigue existiendo igual, sin tocarse.
  - **Bug real encontrado y evitado antes de que llegara a producción**: la imagen Docker del backend (`eclipse-temurin:17-jdk`, base Ubuntu) no traía ningún navegador — y al probar instalar `chromium` vía `apt-get`, resultó ser solo un paquete "stub" que exige `snap` para funcionar, y `snap` no corre dentro de contenedores. O sea que el job programado habría fallado silenciosamente cada noche en el servidor, aunque en local (con Chrome instalado en Windows) todo se viera perfecto. Se cambió la imagen final a `eclipse-temurin:17-jdk-alpine` (Alpine sí tiene un paquete real de `chromium`/`chromium-chromedriver`, sin depender de snap) y se verificó con un build Docker completo + un lanzamiento real de Chromium headless dentro del contenedor que sí funciona. `SeleniumService` ahora lee `CHROME_BIN`/`CHROMEDRIVER_BIN` (variables puestas en el `Dockerfile`, no en `.env`, porque son intrínsecas a la imagen) para apuntar al binario instalado en el contenedor; en desarrollo local (sin esas variables) seguirá usando la resolución automática de Selenium como hasta ahora.
- **Revisión de variables `${VAR}` en `application.properties`**: confirmado que todas (DB, Siigo, JWT, admin seed, upload dir) tienen valor por defecto (`${VAR:...}`) — no hay ninguna variable "desnuda" que pueda repetir el crash-loop del 29-ago.
- **Fix crítico de producción (2026-08-29): backend en crash-loop en el servidor.** Causa: `admin.seed.email`/`admin.seed.password`/`siigo.email`/`siigo.password` en `application.properties` (AutoFacApi) no tenían valor por defecto (`${ADMIN_EMAIL}` en vez de `${ADMIN_EMAIL:}`) — como esas variables no estaban en el `.env` del servidor, Spring fallaba al arrancar el bean `adminSeeder` (dependencia de `SeleniumService`, que se crea siempre al iniciar, no solo al capturar). Se agregó el `:` vacío a las 4 líneas. **Importante:** se comprobó con los logs de MySQL (`docker logs autoFac_mysql`) y consultando la tabla `documento` directo (`docker exec ... mysql ...`) que **los datos nunca se perdieron** — el volumen `autofacinfra_mysql_data` persistió todo el tiempo; el síntoma de "no aparecen los documentos" era 100% causado por el backend caído, no por pérdida de datos. Se agregaron `ADMIN_EMAIL`/`ADMIN_PASSWORD` al `.env` del servidor para que el `AdminSeeder` sembrara el admin de producción.

- **CRUD completo de `registros` (facturas capturadas)**: backend `PUT`/`DELETE /api/data/{id}` + validación de duplicados (`DataService` rechaza con `409` si el número de factura ya existe en otro registro, y `save()` fuerza `id = null` para evitar pisar registros por accidente) + **paginación** (`Page<Data>`, `GET /api/data/list?pagina=&tamano=`, controles Anterior/Siguiente en el frontend). Frontend: un mismo formulario sirve para **crear y editar** (`modoCreacion`), con las fechas usando `<input type="date">` (con conversión automática del formato `DD-MM-AA` de Siigo a ISO al editar registros viejos), "Forma de pago"/"Medio de pago" como `<select>` con valores estándar (antes texto libre), validación obligatoria por campo con mensaje específico bajo cada uno, validación de formato numérico en `valorTotal`, botones Editar/Eliminar de la tabla wireados, mensaje de error de duplicado, y el botón "Nuevo Registro" `sticky bottom-0`. Se corrigió también el bug de change detection en `registros` y `captura` (mismo patrón de `documentos`/`login`: `ChangeDetectorRef.detectChanges()` tras el `subscribe()`) — `home` no lo tiene porque hoy es un placeholder estático sin llamadas HTTP.
- **Bug real encontrado y corregido**: si ya existían dos registros con el mismo `numeroFactura` en la BD (de antes de agregar la validación de duplicados), editar cualquiera de los dos reventaba con un `403` sin mensaje — `iDataRepository.findByNumeroFactura` devolvía `Optional<Data>` pero encontraba 2 filas. Se cambió a `findAllByNumeroFactura` devolviendo `List<Data>`, robusto sin importar cuántos duplicados existan.
- **Normalización de datos**: se corrigieron 8 registros con valores "sucios" de `formaPago`/`medioPago` (ej. `"Credito"` sin tilde, strings mezcladas de Siigo con el monto embebido) a los valores estándar del nuevo `<select>`. Quedaron 3 registros con datos ambiguos que no se tocaron — ver pendiente arriba.
- **Responsive del nav de la landing**: botón hamburguesa (`landing-nav-toggle`, ícono `menu`/`close`) que despliega `landing-nav-mobile` con los 4 links + Dashboard/Cerrar sesión (o Ingresar) en columna, superpuesto debajo del nav; se cierra al hacer clic en cualquier link o en el botón. Corregido bug donde "Dashboard"/"Cerrar sesión" seguían mostrándose como botones completos en mobile (empujaban el toggle fuera de pantalla) — ahora esas acciones solo viven dentro del menú hamburguesa en mobile/tablet (`landing-nav-auth-desktop` oculto hasta `lg`). Punto de quiebre subido de `md` (768px) a `lg` (1024px) para que tablets también vean el hamburguesa, no solo celulares.
- **Modal de vista previa de documentos**: componente compartido `DocumentoPreviewComponent` (público y admin) — trae el archivo como Blob vía `HttpClient` y lo muestra con `<img>` (imágenes) o `<iframe>` (PDF, con `DomSanitizer`); Word/Excel/otros muestran mensaje de "no se puede previsualizar". Modal centrado, overlay oscuro, cierra con el botón o clic afuera. Se corrigió de paso un bug real: `DocumentoController.descargar()` siempre mandaba `Content-Type: application/octet-stream` sin importar el archivo — ahora usa el tipo real guardado, necesario para que la previsualización renderice bien.
- **Optimización de imágenes al subir documentos**: `DocumentoService` usa Thumbnailator para re-codificar automáticamente cualquier imagen subida a JPG (máx. 1920px de lado más largo, calidad 80%) — probado con un archivo real de 2MB que quedó en ~550KB (72% más liviano). PDF/Word/Excel/etc. no se tocan. Nota: PNGs con transparencia pierden esa transparencia al convertirse a JPG (aceptado por ahora).
- **(Backend) Manejo de errores centralizado**: nuevo paquete `exception` con `RecursoNoEncontradoException` (404) y `GlobalExceptionHandler` (`@RestControllerAdvice`) que maneja `RecursoNoEncontradoException` → 404, `IllegalStateException` → 409, y cualquier otra excepción no prevista → 500 con mensaje genérico (antes salía como un `403` vacío y confuso). Se quitó el `@ExceptionHandler` duplicado que tenía `DataController` y `DataService.actualizar()` ahora usa `RecursoNoEncontradoException` en vez de `NoSuchElementException`. Verificado con `curl`: `PUT` a un id inexistente → `404` con mensaje; `PUT` con número de factura duplicado → `409` con mensaje.
- **Crear y editar documentos unificados en una vista aparte**: se eliminó la barra fija de "seleccionar archivo + subir" — ahora solo hay un botón "Subir documento" que abre la misma tarjeta centrada (`admin-documentos-edit-page`/`-card`) usada para editar, con `modoCreacion` para diferenciar (sin campo de nombre al crear, ya que el nombre sale del archivo; con nombre editable + archivo opcional al editar). Mismo patrón que `registros`.
- **Los 3 registros con datos ambiguos, resueltos**: `FE 475B` (duplicado de `FE 475`, NATURLAB SAS) eliminado por DB; `FE 500` corregido (`Contado`/`Transferencia`); `FE 120` resultó que el usuario ya lo había borrado probando el botón "Eliminar" — no era ningún bug.
- **Responsive de documentos**: la tabla admin (`admin/documentos`) no tenía ningún tratamiento responsive — se le aplicó el mismo patrón de `registros` (`hidden lg:table` + lista de tarjetas apiladas `admin-documentos-mobile-list` para tablet/celular). La grid pública ya estaba bien (1 columna por defecto, se expande desde `sm`), no se tocó.
- **(Backend) CORS a variables de entorno**: `SecurityConfig` ahora lee `cors.origins` (`${CORS_ORIGINS:http://localhost:4200,https://autofac.modoblu.com}`) en vez de tener las URLs escritas en el código — cuando cambie el dominio (recordar `modoblu.com` vence ~sep. 2026), solo hay que actualizar `CORS_ORIGINS` en el `.env` del servidor, sin recompilar.
- **Responsive de la tabla de `registros`**: punto de quiebre subido de `md` (768px) a `lg` (1024px) para que tablet también vea las tarjetas en vez de la tabla de escritorio. Se encontró y corrigió un conflicto real: la clase `.mobile-container` traía su propio `md:hidden` incrustado en `styles.css` que competía con el `lg:hidden` puesto en el HTML — sin ese fix, tablet se quedaba sin ver ni la tabla ni las tarjetas.
- **Favicon personalizado**: logo del usuario en `assets/img/favicon.jpeg`, `index.html` actualizado (`<link rel="icon" type="image/jpeg" href="assets/img/favicon.jpeg">`). El `favicon.ico` genérico de Angular sigue en `src/` sin usarse — pendiente decidir si se borra.
- **Landing: sacar todo Tailwind suelto del HTML a `styles.css`**: las secciones "Quiénes somos", "Por qué elegirnos", "Sectores que atendemos", CTA y footer tenían clases de Tailwind directo en el template (solo el hero ya estaba centralizado). Se crearon ~25 clases `.landing-*` nuevas y se dejaron `stagger-group`/`stagger-item`/`hero-animate` intactas (son selectores que usa GSAP en `landing.component.ts`, no clases de estilo). De paso se corrigió que el link "Quiénes somos" del nav apuntaba a `#quienes-somos`, un `id` que nunca existía en esa sección.
- **(Backend) Lombok aplicado a las entidades**: `Documento`, `Data` y `Usuario` ahora usan `@Getter`/`@Setter` en vez de getters/setters manuales (los DTOs `LoginRequest`/`LoginResponse` ya los usaban desde que se crearon). Confirmado con `./mvnw.cmd clean compile` sin errores.
- **Variables CSS en `styles.css`**: agregado un bloque `:root` al inicio con la misma paleta de `tailwind.config.js` (`--color-blue-main`, `--color-primary`, etc.), y reemplazadas las 6 repeticiones hardcodeadas de `#008abb` en el sidebar/toggle por `var(--color-blue-main)`.
- **Botones "volver" uniformados**: renombrada `.documentos-back-link` a una clase compartida `.back-link` (pill, ícono `arrow_back`, sombra en hover) y aplicada también en `login.component.html` (antes `.login-back-link`, solo texto con "←"). Cualquier "volver" nuevo debe usar `.back-link`.
- **SweetAlert2 instalado y reemplazando `confirm()` nativo**: en `admin/documentos` y `registros`, el `eliminar()` ahora usa `Swal.fire()` con ícono de advertencia (en vez del diálogo feo del navegador), y también muestra un aviso de error si la petición falla (antes fallaba en silencio). Confirmado con `ng build` sin errores.
- **Refactor completo `*ngIf`/`*ngFor` → `@if`/`@for`**: era el único archivo que faltaba (`admin/documentos/documentos.component.html`), ya migrado. Confirmado con búsqueda en todo `src/app` que no queda ninguno.
- **Botón "Volver al inicio" en `/documentos` (público)**: link con `RouterLink` a `/` arriba del título, con diseño de botón real (pill, borde, ícono `arrow_back`, sombra en hover).
- **Captura de Siigo: manejo correcto de duplicados**: `DataController.capturar()` usa el mismo `DataService.save()` que ahora valida duplicados — si la factura ya existe, se rechaza con `409` y un mensaje claro en vez de crear basura. En el frontend (`captura.component.ts`), ese caso ya no se ve como un error genérico: muestra el mensaje real del backend en un aviso ámbar. De paso se corrigió un bug donde el mensaje de error se mostraba con la clase `.success` (verde) — ahora tiene su propia clase `.captura-alert-error`.
- **Login + autenticación completos (backend y frontend)**: `POST /api/auth/login` genera JWT real (Spring Security + JWT), `iUsuarioRepository`/`Usuario`/`UsuarioDetailsService`/`JwtService`/`JwtAuthFilter`/`SecurityConfig`. Frontend: `AuthService`, `authInterceptor`, `authGuard` (bloquea `/admin/**` sin sesión, redirige a `/admin/login`), `LoginComponent` con validaciones. Verificado de punta a punta con Insomnia y por UI.
- **Reorganizar "Home" del panel admin** (hecho por el usuario): `HomeComponent` ahora es un dashboard placeholder (tarjetas de resumen), y la captura de facturas Siigo se movió a su propio componente `CapturaComponent` con su propio ítem "Captura" en el sidebar (`/admin/captura`).
- **Auto-logout por inactividad + botón "Cerrar sesión"**: `InactivityService` (timer de 15 min, se resetea con mouse/teclado/click/scroll), wireado en `dash-board.component.ts`. Botón de logout como `<div class="sideBar-logout">` independiente (hermano de `<ul>` dentro de `.sideBar`, no como `<li>` del menú) — solo visible si `authService.isAuthenticated()`, con el mismo efecto de curva blanca que el resto del sidebar (arriba y abajo) activado en hover, alineación de ícono/texto corregida. Se corrigió también un bug de scrollbar en el sidebar (`overflow-x: hidden` sin `overflow-y` hace que el navegador calcule `overflow-y: auto` automáticamente — se agregó `overflow-y: hidden` explícito).

- Auditoría de seguridad inicial (credenciales fuera del código), actualización de Angular 15 → 22, separación de rutas público/admin, despliegue Docker funcionando en el servidor (ver plan principal para el detalle completo).
- Módulo `documentos` (público): backend completo (subir/listar/descargar) + grid pública de archivos funcionando.
- **Hero de la landing rediseñado** (imagen real a pantalla completa, nav superpuesto dentro de la sección, sin el marco celeste que venía del `app-bg` global). Se movió `app-bg` de `app.component.html` (envolvía toda la app) a `dash-board.component.html` (solo el panel admin lo necesita). Nuevas clases `.landing-*` centralizadas en `styles.css`.
- **CRUD completo de documentos (admin)**: agregado `eliminar` (borra archivo físico + fila en BD, en ese orden para no dejar basura) y `actualizar` (renombrar y/o reemplazar el archivo) en backend (`iDocumentoService`, `DocumentoService`, `DocumentoController` — `DELETE`/`PUT /api/documento/{id}`) y frontend (`DocumentosService.eliminar()`/`actualizar()`, componente `admin/documentos` con panel de edición inline y botones Editar/Eliminar en la tabla). Eliminar/actualizar solo existen en la vista admin, nunca en la pública — el backend en sí sigue sin autenticación (decisión ya tomada, se protege de verdad en el Paso 4/5).
- **Bug del input de archivo corregido**: usando `@ViewChild` + `nativeElement.value = ''` para limpiar el `<input type="file">` nativo tras subir/editar con éxito (tanto en el input de "subir nuevo" como en el del panel de edición).
- **Módulo del blog de la universidad**: hecho (confirmado por el usuario).
